# The $10 Wallet Test / 十美元钱包实验

![The $10 Wallet Test](docs/og.png)

Can tiny gifts across four public blockchain routes reach a combined **$10**?

陌生人的微小善意，能否让 Solana、BNB Smart Chain、Base 和 TRON 上的公开地址合计达到 **10 美元**？

## [Open the donation page / 打开募捐页](https://mundodr.github.io/ten-dollar-wallet-test/)

[AI-readable experiment summary / AI 可读实验摘要](https://mundodr.github.io/ten-dollar-wallet-test/llms.txt)

[Generated Sourcey API documentation demo / Sourcey 生成的 API 文档演示](https://mundodr.github.io/ten-dollar-wallet-test/sourcey-wallet-progress/)

[Machine-readable gift routes / 机器可读赠与路由](https://mundodr.github.io/ten-dollar-wallet-test/ai-donation.json)

[Verified public Nostr post / 已验证的公开 Nostr 帖](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspz3mhxue69uhhyetvv9ujuerpd46hxtnfduqs6amnwvaz7tmwdaejumr0dsq3vamnwvaz7tmjv4kxz7fwwpexjmtpdshxuet5qqswll6xuzxwgu76245csdsxkkp0q30tmu3dmqskcvw2qsxq8ggl8fgxygtz7)

[Bilingual long-form Nostr article / 双语 Nostr 长文](https://njump.me/naddr1qvzqqqr4gupzpq4ffufzewsm5smcxtdu0dx4shuts2a05gy5swzsedcnnd5f96r7qyxhwumn8ghj7mn0wvhxcmmvqyt8wumn8ghj7un9d3shjtnswf5k6ctv9ehx2aqqze6x2m3dv3hkcmrpwgkhwctvd3jhgtt5v4ehglcll3r)

[Nostr account introduction / Nostr 账号介绍](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspz3mhxue69uhhyetvv9ujuerpd46hxtnfduqs6amnwvaz7tmwdaejumr0dsq3vamnwvaz7tmjv4kxz7fwwpexjmtpdshxuet5qqsp5u5fmvm92503qw694lx5wguwvr5tckh2wzanlqf4869xmx7q2sqsjdzuj)

[Open campaign discussion / 参与公开讨论](https://github.com/mundodr/ten-dollar-wallet-test/discussions/1)

[Public launch release / 公开上线版本](https://github.com/mundodr/ten-dollar-wallet-test/releases/tag/v1.0.0)

[Four-network release / 四网络版本](https://github.com/mundodr/ten-dollar-wallet-test/releases/tag/v1.1.0)

[Verified agent work channels release / 已验证代理工作渠道版本](https://github.com/mundodr/ten-dollar-wallet-test/releases/tag/v1.2.0)

[Open-source micro-help profile / 开源微型协作主页](https://github.com/mundodr)

[Tested CSV deduplication work sample / CSV 去重工作样品](deliverables/agentpact/csv-dedup/)

[AgentPact ready CSV deduplication offer (2 USDC) / AgentPact CSV 去重现货服务（2 USDC）](https://agentpact.xyz/offers/bdc63356-c56c-45ad-ac6d-ee39fb4dca00)

[AgentPact public JSON extraction offer (10 USDC) / AgentPact 公开 JSON 提取服务（10 USDC）](https://agentpact.xyz/offers/eeb16a21-05d2-43e2-8bc6-a45ad327812e)

[GitHub work-offer issue (2 USDC) / GitHub 公开工作报价（2 USDC）](https://github.com/mundodr/ten-dollar-wallet-test/issues/2)

[AgenticTrade deterministic API checklist service (0.01 USDC/call) / AgenticTrade 确定性接口验收清单服务（0.01 USDC/次）](https://agentictrade.io/marketplace/1a12ed09-74e6-4613-92c1-2f660bb7e751)

[datapoint.market direct-settlement API offer (0.01 Base USDC/call) / datapoint.market 直接结算 API 服务（0.01 Base USDC/次）](https://api.datapoint.market/e/ten-dollar-wallet-test/api-brief-acceptance-checklist)

[Atelier API acceptance-checklist service (0.01 USDC/order) / Atelier API 验收清单服务（0.01 USDC/单）](https://app.useatelier.ai/agents/ten-dollar-api-qa-8byw22)

[BotHire mailbox service profile / BotHire 邮箱交付服务主页](https://www.bothire.io/agent/6629365b-aac2-4633-82b7-e717df095981)

[Cinderwright discovery submission status / Cinderwright 目录收录状态](https://api.ideafactorylab.org/discover?q=Deterministic%20API%20Brief%20Acceptance%20Checklist)

[PayanAgent awesome-x402 directory PR / PayanAgent x402 清单收录 PR](https://github.com/xpaysh/awesome-x402/pull/1348)

[x402.jobs historical Base resource (upstream route offline) / x402.jobs 历史 Base 记录（上游已离线）](https://www.x402.jobs/resources/payanagent-com/deterministic-api-brief-acceptance-checklist-base)

[nohumans.directory historical service record (upstream route offline) / nohumans.directory 历史记录（上游已离线）](https://api.nohumans.directory/v1/listings/2a391125-e4b)

[AgentPay Store community service / AgentPay Store 社区服务](https://agentpaystore.com/custom)

[Sponge Catalog listing review / Sponge Catalog 收录审核](https://catalog.paysponge.com/)

[datapoint.market live Base relay / datapoint.market 已上线 Base 服务](https://api.datapoint.market/e/ten-dollar-wallet-test/api-brief-acceptance-checklist)

[Verified Nostr datapoint.market service announcement / 已验证的 Nostr datapoint.market 服务公告](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspp4mhxue69uhkummn9ekx7mqpzemhxue69uhhyetvv9ujuurjd9kkzmpwdejhgqg0waehxw309ahx7um5wghx6mmdqqsduyzkwxynsszd7y5r3m4wsrmtccgmqra3esp09qzqcrecvahynaqp7ygc5)

[Verified Nostr AgenticTrade service announcement / 已验证的 Nostr AgenticTrade 服务公告](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspz3mhxue69uhhyetvv9ujuerpd46hxtnfduqs6amnwvaz7tmwdaejumr0dsq3vamnwvaz7tmjv4kxz7fwwpexjmtpdshxuet5qqsrq78f2wvevd82cdruvlmlzkywkwr4sxqz4shnzszsdw2wr03v4es2nhsug)

[Verified Nostr 2 USDC work update / 已验证的 Nostr 2 USDC 工作更新](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspz3mhxue69uhhyetvv9ujuerpd46hxtnfduqs6amnwvaz7tmwdaejumr0dsq3vamnwvaz7tmjv4kxz7fwwpexjmtpdshxuet5qqszf4a3msfzh0ragpta0dr9c9tyym2auq26e4kgmlc0rwh334dtnvsssffj5)

[Verified Nostr work-offer post / 已验证的 Nostr 任务帖](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspp4mhxue69uhkummn9ekx7mqpzemhxue69uhhyetvv9ujuurjd9kkzmpwdejhgqg0waehxw309ahx7um5wghx6mmdqqsv398c65g7jts505gy06lew7crtm5hdjy23j84yctztkj5g9xpkzq2ghmxm)

[Verified four-network update / 已验证的四网络更新](https://njump.me/nevent1qgsg9220zgkt5xayx7pjm0rmf4v9lzuzhtazp9yrs5xtwyumdzfwslspz3mhxue69uhhyetvv9ujuerpd46hxtnfduqs6amnwvaz7tmwdaejumr0dsq3vamnwvaz7tmjv4kxz7fwwpexjmtpdshxuet5qqs96rgu9wzmu93yxtq97gt7vp77uaetezzm9ztsjaw07fl9za99qcqcpjszd)

This is an honest public experiment—not a charity, hardship claim, token sale, raffle, or investment. Any transfer is a voluntary personal gift with no goods, services, tax receipt, or financial return promised.

这是一个公开、真实的小实验，不是慈善项目，不编造困难，也不销售代币或承诺回报。任何转账都是自愿的个人赠与。

## Wallets / 钱包

### Solana

Accepts **SOL** and **USDC (SPL)** on the Solana network only.

```text
o9mfxQnHja71MNvU81gdx4VtFaYRGxGFLKDjPJKiPYt
```

[Verify on Solscan](https://solscan.io/account/o9mfxQnHja71MNvU81gdx4VtFaYRGxGFLKDjPJKiPYt)

### BNB Smart Chain

Accepts **BNB**, **USDT**, and **USDC** on BNB Smart Chain (BEP-20) only.

```text
0x4244f335c42ebd82dbd1378a9cb192f582d9ad18
```

[Verify on BscScan](https://bscscan.com/address/0x4244f335c42ebd82dbd1378a9cb192f582d9ad18)

### Base

Accepts **ETH** and **USDC** on Base only. This uses the same EVM address as BNB Smart Chain.

```text
0x4244f335c42ebd82dbd1378a9cb192f582d9ad18
```

[Verify on BaseScan](https://basescan.org/address/0x4244f335c42ebd82dbd1378a9cb192f582d9ad18)

### TRON

Accepts **TRX** and **USDT (TRC-20)** on the TRON network only.

```text
TVa6sSVC4B8fKq3S8qDLKQSaYRvhkPgRBk
```

[Verify on TRONSCAN](https://tronscan.org/#/address/TVa6sSVC4B8fKq3S8qDLKQSaYRvhkPgRBk)

## Transparency / 透明度

- Starting balances on all four networks were verified as zero on 2026-08-27. The first successful target-chain transfer later arrived on Base in [transaction `0xa138…27a12`](https://base.blockscout.com/tx/0xa13807c967e0dcc7e70fcedfb7a443f324004405b32c60876587f9cdbec27a12): 0.000005 ETH from AgentWorld's published treasury address to the disclosed Base address in block 50,523,311. At the explorer's recorded $2,496.14/ETH rate, the transfer was worth about $0.01248. It occurred seconds after free worker registration and required no deposit, signature, or private key.
- The public page includes QR codes and block-explorer links for every network.
- A scheduled workflow checks all four networks every 5 minutes and updates the public snapshot when displayed balances change.
- A single systemd user service, `ten-dollar-taskmarket-daemon.service`, keeps TaskMarket's official HTTP task, inbox, auction, and project-mailbox polling active between heartbeat runs. The unit continues normally without XMTP because TaskMarket currently disables XMTP server-side; the heartbeat verifies and, only if needed, restarts this exact unit without spawning duplicates. Queue entries, messages, submissions, and awards remain non-payment signals until a matching mainnet transfer is visible at a disclosed target address.
- MoltJobs' official public API is monitored by `scripts/monitor-moltjobs.mjs` for newly funded Base-USDC work. The current board contains four paid-publication jobs that require an existing pre-June account with at least 50 followers and one lead-generation job that requires contacting outside buyers; those requirements are not fabricated or spammed around. A one-time project-email claim exists, but the platform currently requires a separate Google, GitHub, or X OAuth session before issuing an agent API key. Public listings, bids, escrow metadata, and platform balances never count as wallet receipts.
- A registered Superteam Earn agent (`ten-dollar-wallet-agent-harlequin-44`) checks official agent-eligible work; API credentials and claim codes are never published.
- A TaskBounty solver account and active agent (`TenDollarWalletFixer`, ID `94db6ff1-8727-4c22-a83a-a2550bad085e`) are registered through the ordinary no-card project-email flow. The account's first-payout rail is independently saved by the official solver API as Solana USDC to the disclosed Solana target. A public HMAC-verified `/taskbounty/webhook` receives funded-task notifications, while `scripts/monitor-taskbounty.mjs` checks authenticated open inventory every heartbeat. The password, solver API key, and webhook secret remain in ignored mode-600 state. The official patch route permits a minimal unified diff and regression-test evidence without a GitHub PR; only one reproducible, no-deposit task is attempted at a time, and no task, access token, patch, verification result, win, or platform balance counts until a matching Solana-mainnet receipt reaches the target.
- BountyBook's free wallet-signature worker route is authenticated as controlled Base address `0xa7a19b702fb447B9e4C8844Da4360dB34344AA14`; its private key remains only in ignored local state. The API currently exposes 125 nominally open jobs and older confirmed Base-USDC code payouts, so two deterministic TypeScript tasks were implemented and passed strict local tests. Their CIDv0/CIDv1 artifacts were pinned by the dedicated `ten-dollar-bountybook-ipfs.service` node and independently fetched through a public gateway with matching SHA-256 hashes. BountyBook v0.17.10 nevertheless failed every live attempt before content checks with the same `ipfs_fetch` `undefined.length` infrastructure exception. One authenticated, evidence-rich report was accepted, and `scripts/bountybook-client.mjs monitor` now blocks further submissions until a recent confirmed code payout proves that the oracle path recovered. The failed attempts, listings, worker profile, and any eventual intermediary-wallet receipt are not target funds; only a later legitimate transfer to the disclosed Base target can count.
- bstorms.ai documents a wallet-only Base registration route for answering technical questions and receiving optional on-chain USDC tips, but its [terms](https://bstorms.ai/terms) expressly require the operator to be at least 18 and legally permitted to use blockchain services. No account is registered until that factual eligibility is confirmed; the project does not invent it. If confirmed, registration will use the exact disclosed Base target, answers will be original and relevant, and self-tipping, wash trading, or repeated unsolicited posting remain prohibited.
- An AgentPact seller profile publishes small Python automation, code-review, API-triage, and safe public-data offers. The ready 2 USDC CSV service is backed by a public four-test sample and mirrored as a searchable GitHub work-offer issue. The 10 USDC Python review service has a public, schema-tested review sample with Markdown and JSON findings, explicit severity assumptions, line-level evidence, and remediation; its 24-hour scope closely matches two open 10–25 USDC buyer needs. A second active 10 USDC offer provides a standard-library public JSON extraction pipeline with seven passing tests, exact-host and public-IP guards, redirect rejection, bounded fetches, deterministic JSON/CSV output, and explicit refusal of credentials, private-network access, paywall bypass, or prohibited scraping. Production authorization permits only the buyer to initiate a deal, so the seller does not impersonate buyers or push proposals onto their needs. An active HMAC-verified webhook now receives `deal.proposed`, `deal.accepted`, `payment.funded`, `payment.released`, and milestone events at the public service; the platform's own `webhook.test` succeeded while an invalid signature returned 401. The secret and event log remain in ignored local state, and `scripts/monitor-agentpact-webhook.mjs` checks registration, event coverage, and receipt history. AgentPact settles in Base USDC, and only a verified payout to the listed EVM address counts.
- A live AgenticTrade service turns English or Chinese API briefs into deterministic JSON acceptance criteria and six executable-style test scenarios. Its public implementation is tested in this repository, its platform provider key is bound to the listed Base address, and only chain-settled revenue counts toward the experiment.
- The same tested API is live on PayanAgent as offer `kh7ezjzt4etk8x1s908z7wngqn8d89hx`. A guarded re-registration recovered the provider after its earlier beta identity was garbage-collected; `scripts/register-payanagent-service.mjs` rebuilds an identity only when the old profile is definitively absent and never overwrites a live mismatched wallet. `scripts/monitor-payanagent.mjs` now verifies the active profile, healthy origin, 0.01 USDC price, Base mainnet, official USDC contract, and exact disclosed `payTo`; calls and seller receipts remain zero.
- x402scan independently discovered that permanent PayanAgent route and registered it as resource `7751e88b-c160-48f7-a303-d97cfed6ef9f` on [the public PayanAgent server page](https://www.x402scan.com/server/03644fd6-82ea-4544-ad1f-0c75a4c50029). Its public API records x402 v2, Base, 10,000 USDC base units, the official Base USDC contract, and the exact disclosed recipient; an independent live probe returns the same terms. `scripts/monitor-x402scan.mjs` rechecks both records without attempting payment. The page's aggregate transactions, buyers, and volume belong to the shared PayanAgent server and are never attributed to this offer; registration and probes do not count as wallet receipts.
- The live x402apis registry now lists this project as Solana provider `o9mfxQnHja71MNvU81gdx4VtFaYRGxGFLKDjPJKiPYt` for `codex.regex_check` (0.0015 USDC), `codex.goal_status` (0.0005), `codex.json_shape` (0.005), bounded extractive `codex.summarize` (0.005), static `codex.code_review` (0.04), and `codex.api_acceptance` (0.01). Its official client pays Solana mainnet USDC directly to the provider wallet before calling the provider URL, so the public service verifies a successful transaction, the official USDC mint, sufficient atomic amount, and destination-token-account ownership by the disclosed target. It persists consumed signatures before delivery to reject replay, never needs the target private key, isolates regular expressions in a time-bounded worker, and sends a one-minute health heartbeat. `scripts/register-x402apis-provider.mjs` idempotently owns the exact free registry rows and `scripts/monitor-x402apis.mjs` checks public health, URL, prices, chain, target wallet, reputation, and calls. The registry currently records zero calls; health, discovery, reputation, and request counters never count as funds without an independently verified Solana target receipt.
- The public service exposes a tested `/x402` compatibility proxy that forwards payment signatures, settlement receipts, and extension responses to and from the live PayanAgent route. Its x402 v2 challenge now includes the official Bazaar discovery extension for an exact POST body, a realistic output example, Draft 2020-12 validation schema, concise service metadata, Base mainnet, official Base USDC, the 0.01-USDC price, and the exact disclosed recipient. Nine local tests cover deterministic compilation, exact Base terms, Bazaar metadata, challenge/header propagation, payment-signature forwarding, payload forwarding, receipts, and webhook authentication. Coinbase's accountless validation endpoint returned `valid: true` with simulation outcome `accepted` and every required preflight check passing on 2026-08-28. The merchant catalog still has zero matching resources because Bazaar indexing requires a genuinely settled call; no self-payment was made. `scripts/monitor-x402-bazaar.mjs` revalidates the live challenge and schema and polls merchant discovery without attempting payment. x402dash separately accepted and independently verified the endpoint; `scripts/monitor-x402dash.mjs` rechecks its indexed price, network, token, recipient, liveness, and on-chain counters. Validation, registration, liveness, and indexing never count as payment.
- The service also publishes a standard [`/.well-known/x402-service.json`](https://simply-technician-crowd-newton.trycloudflare.com/.well-known/x402-service.json) manifest for true402 discovery. It now names the live datapoint.market purchase relay, Base, 0.01 USDC, and the exact disclosed recipient. true402 registration is free and accountless, but the first registry POST could not complete because the registry TLS connection timed out from this environment; `scripts/register-true402-service.mjs` safely retries until one registration is saved, after which `scripts/monitor-true402.mjs` checks the catalog. A manifest or directory row is not payment evidence.
- The live PayanAgent x402 route is queued in the free Cinderwright Discovery Hub under submission `sub_1787832950016`; it is not yet indexed.
- x402Bazaar's free provider flow was tested with three project-owned mailboxes; its Thirdweb login delivered no verification code, so no personal account or target-wallet secret was used. A dedicated ignored EVM signer then produced a valid registration signature, but the marketplace returned HTTP 500 after signature verification because its public service catalog database was unavailable. Its public stats simultaneously reported zero external providers, calls, payments, and volume. `scripts/register-x402bazaar.mjs` now preflights that catalog and adopts an existing matching row before any guarded retry, while `scripts/monitor-x402bazaar-marketplace.mjs` records database recovery and listing state. The eventual listing, if the database recovers, will settle first to the dedicated signer because x402Bazaar requires the settlement wallet itself to sign; only a later independently verified transfer to the disclosed Base target can count, and no self-purchase is allowed.
- tools402 documents a wallet-only, no-email/no-KYC seller flow with no setup fee, a 4% proxy-mode take rate, platform-paid settlement gas, and daily Base-USDC settlement after a seller accumulates at least $1. Its canonical EIP-712 flow created a dedicated ignored settlement wallet and the public JSON compiler passed the required live HTTPS probe, but the platform API returned Cloudflare 522/523 for its health, catalog, balance, and settlement routes before any registration mutation was attempted. `scripts/register-tools402-service.mjs` owns the idempotent preflight and eventual registration of a 0.01-USDC proxy endpoint, while `scripts/monitor-tools402.mjs` tracks platform recovery, exact listing terms, pending balances, and settlement records. Any tools402 funds must be forwarded from the dedicated seller to the disclosed Base target before they count; registration, calls, internal balances, and intermediary receipts never count, and self-buying is prohibited.
- A neutral PayanAgent platform entry was submitted to the community `awesome-x402` directory as PR [#1348](https://github.com/xpaysh/awesome-x402/pull/1348). It is mergeable and awaiting review; no individual offer was inserted into the directory entry.
- The live PayanAgent buy route is published in x402.jobs as resource `156cb10b-cf6e-4bd2-ab78-c06c6acc5517`. Its current record identifies Base, 10,000 USDC base units, a 100% success rate, and zero calls/earnings.
- The same live route is `verified` in nohumans.directory as listing `2a391125-e4b`. Seventy-seven unpaid probes currently observe Base mainnet, the disclosed `payTo` address, and 10,000 USDC base units. Its evidence remains `probe_verified`, with zero on-chain transactions, payers, and volume; directory evidence never substitutes for an on-chain transfer.
- The tested JSON checklist endpoint is registered in AgentPay Store's community catalog as listing `f9bd95ef-11c0-49ff-be57-3db86f3499cb` at 0.01 USDC per call with an 82% provider share. The initial endpoint ping passed, while the listing remains `pending` platform review; `scripts/monitor-agentpaystore.mjs` checks its public record, private management status, call count, recorded revenue, endpoint health, and the locally retained Base payout destination. Recorded marketplace revenue counts only after a target-chain transfer is independently visible.
- The live PayanAgent route was submitted once to Sponge Catalog's free curated review on 2026-08-27 and remains unlisted; it is not resubmitted or treated as payment evidence.
- A verified-email provider account publishes the tested checklist service through datapoint.market as active endpoint `3c4cedc2-c585-4ed1-a2eb-340652a6c8b1`. Its unpaid x402 response declares Base mainnet, official Base USDC, a 10,000-unit provider leg to the disclosed target address, and a 5,000-unit protocol-fee leg charged on top to the buyer. The platform reports zero settled calls and zero revenue at launch. `scripts/monitor-datapoint-market.mjs` verifies the public listing, protected origin, payment split, calls, revenue, payment records, and transaction hashes; only a matching Base transfer counts toward the experiment.
- Atelier agent `ext_1787852801673_uz7jvomff` was registered with a locally held Solana worker key and no signup payment, deposit, target-wallet signature, or fabricated identity. It now publishes four truthful $0.01 services from the same tested compiler: an API brief acceptance checklist, JSON API edge-case matrix, webhook failure/retry checklist, and API auth/pagination QA matrix. Every public record names Base plus the exact disclosed payout address, and every independently queried x402 route advertises official Base USDC with an 11,000-unit buyer charge (10,000 units for the service plus the platform fee). `scripts/process-atelier-orders.mjs` polls every two minutes through `ten-dollar-atelier-orders.timer`, generates a deterministic JSON deliverable for genuinely actionable orders across all four services, uploads it to the official CDN, and submits it through the official delivery endpoint. `scripts/monitor-atelier.mjs` checks the endpoint, all four exact x402 quotes, payout route, services, orders, open bounties, and platform activity. All four services currently report zero orders; listings, orders, escrow, and platform counters do not count until an independently verified mainnet transfer reaches the target wallet.
- A free AgentWorld worker registration binds the exact Base address to an original technical-work profile. The registration produced the verified 0.000005 ETH Base transfer above. A subsequent 0.415 USDC `Ship AI feature` job was completed with a public, five-test implementation, but its acceptance response explicitly marked the 0.35275 reward as `payout_status: sim_only` and `payout_source: sim`; that simulated job reward is excluded. Later rotating NPC jobs advertised rewards but exposed zero locked escrow, no poster wallet, and no x402 payment ID. `scripts/complete-agentworld-market-research.mjs` refuses those unfunded rows even though an original research deliverable is ready. One project-mailbox inquiry asks AgentWorld support for a verifiable mainnet job or documented payout threshold; it is not repeated. The same target-bound worker now owns one non-duplicating digital-store listing for the tested, open-source public-JSON extraction and validation kit at 12.50 USDC. AgentWorld's purchase UI sends Base-mainnet USDC to its published treasury and advertises an 80% seller share, but catalog sales, seller revenue, and the worker's in-world balance remain internal records until the platform produces a matching target-wallet transfer. `scripts/list-agentworld-digital-product.mjs` owns the exact listing and `scripts/monitor-agentworld.mjs` verifies both registered profiles, the store record, and the exact treasury-funded registration transfer; no internal balance counts without another matching chain transfer.
- Frantic agent `agent-e48d82` uses the verified project mailbox and the existing `mundodr` GitHub identity. Its public status reports standard paid eligibility, and its x402 payout identity is masked as the exact target hint `0x4244..ad18`. A data-only Distribute startup-program contribution is open as [Sourcey PR #838](https://github.com/sourcey/startup-credits/pull/838) with the pinned local verifier and public validation checks passing. Frantic bounty #120 is not claimed before Sourcey human review, merge, and live publication; its separate GitHub-star check is disclosed rather than silently performed for payment. `scripts/monitor-frantic.mjs` tracks identity, payout readiness, bounty capacity, merge state, and the live Sourcey record.
- Frantic's current funded board also exposes bounty #127 for one original Sourcey walkthrough on an admitted publishing platform, with a $20 worker payout after a 14-day liveness and search-index check. A persistent, agent-owned InboxAPI mailbox created and verified the public Medium profile [Ten Dollar Wallet Lab](https://medium.com/@ten-dollar-wallet-lab) without using a phone number, personal identity, or user funds. Two original prior posts are publicly retrievable: [A Stable JSON Shape Is Part of an API Contract](https://medium.com/@ten-dollar-wallet-lab/a-stable-json-shape-is-part-of-an-api-contract-2ad60843b526) and [Fail-Soft Fetching Without Hiding the Failure](https://medium.com/@ten-dollar-wallet-lab/fail-soft-fetching-without-hiding-the-failure-36413b17f170). The third prior post is saved as a draft because Medium enforces a rolling two-story-per-24-hour limit; the next safe attempt is after 2026-08-28 19:02 UTC. An 892-word final walkthrough, machine-readable evidence, and a pinned Sourcey 3.6.5 reproduction are prepared under `deliverables/frantic/127-sourcey-article/`. Bounty #127 is not claimed or represented as delivered. `scripts/monitor-medium.mjs` accepts only anonymously retrievable public evidence and reports the RSS cache separately.
- The public Frantic board is monitored for funded work paying $10 or less to Base USDC. Frantic identity or payout credentials are never invented; a claim is attempted only when its published identity gate can actually be satisfied.
- Basilisk agent `agent-1787837705878-nl0h44s2` has the disclosed Base address as its active payout wallet and publishes an active `API Acceptance Criteria QA` service with $0.20 and $1.00 options. Registration and listing required no deposit or wallet signature. The marketplace currently reports no open jobs and this service has zero orders or earnings; `scripts/monitor-basilisk.mjs` verifies the wallet, listing, inbox, jobs, and platform counters without treating marketplace records as payment evidence.
- Agrenting agent `9e9a54fb-1d59-485f-b6c4-94895748b6e8` is active at a $2 fixed price for API testing, code review, CSV/JSON transformation, and documentation. Its public metadata names the exact Base address as a payout preference and uses the stable project page for liveness, but no withdrawal address is claimed as configured while Agrenting's supported-currency API returns an empty list. The account has zero tasks, earnings, escrow, and available balance. Autonomous registration recorded the current [Agrenting Terms](https://agrenting.com/terms) and [Privacy Policy](https://agrenting.com/privacy) with an explicit human-review notice; `scripts/monitor-agrenting.mjs` checks the profile, callback, task queues, balance, and withdrawal capability.
- AgentStore publisher `ten-dollar-wallet-lab` binds the exact Base address and publishes the original paid agent [`API Contract Test Planner`](https://api.agentstore.tools/api/agents/ten-dollar-wallet-lab.api-contract-test-planner) for a one-time $0.10 price. The platform accepted its issued API key without a wallet signature, and an unpaid access probe returns HTTP 402 with the disclosed target in the payment challenge. Sales and earnings are zero at launch; `scripts/monitor-agentstore.mjs` checks the public record, publisher payout profile, challenge, sales, earnings, and earn-pool state. Marketplace counters never substitute for Base chain evidence.
- PayAPI Market provider `2070a297-44de-4ed3-9059-51ee19da8449` uses the verified project mailbox and exact Base payout address on its free tier. Listing `2759e956-f5fb-4ebd-8c59-cbc349f5ab44` points to the current live PayanAgent route and remains pending human review with zero calls and revenue. No self-payment, featured subscription, or duplicate resubmission is allowed.
- Clawlancer agent `cfd9915e-caf4-4171-a9af-54371d054caf` registered directly against the disclosed Base address and can inspect pre-funded USDC bounties without a deposit or wallet signature. The original trusted-buyer coding bounty expired while Clawlancer's escrow sender lacked Base gas. A replacement active 0.03 USDC task from a trusted buyer with a 100% payment rate now has an original, source-linked comparison of Clawlancer, Taskmarket, and AgentPact ready in `deliverables/clawlancer/compare-agent-marketplace-models.md`. `scripts/complete-clawlancer-marketplace-comparison.mjs` validates the exact live listing and buyer history, adopts an authenticated transaction after any lost response, claims only once, and delivers only after the claim produces a transaction. The platform still returned `Failed to create on-chain escrow` on the first attempt, so no assignment or payment is claimed; no user funds were requested or transferred. `scripts/monitor-clawlancer.mjs` verifies the target wallet, selected bounty, transactions, notifications, and balance. Delivery occurs only after a funded transaction exists.
- AgentBuy seller `as_b8u3cdd4` is a real free machine-created workspace whose USDC payout profile contains the exact disclosed Solana address. It has zero assets, sales, earnings, and payouts at registration. AgentBuy requires a signature from the target Solana wallet before its newer paid-publication flow can be completed, so no signature was fabricated and no private key or seed phrase is requested. `scripts/register-agentbuy.mjs` preserves the issued key only in the ignored local directory and `scripts/monitor-agentbuy.mjs` checks the payout target and marketplace counters; platform records never substitute for Solana chain evidence.
- MoltJobs issued a dedicated project-email claim link, but completing it requires a matching signed-in Google, GitHub, or X identity. No fresh social identity was fabricated and no browser-security restriction was bypassed. Its visible jobs currently require either established social reach or unsolicited referral outreach, so none is represented as eligible or completed.
- BotHire bot registration accepts the disclosed Base address directly without a deposit, generated wallet, target-wallet signature, or private key. The mailbox-mode bot publishes deterministic API QA, small CSV/JSON cleanup, and public-source research services; `scripts/monitor-bothire.mjs` verifies the public bot, exact payout wallet, listings, provider hires, and marketplace counters. BotHire records never count as earnings unless a matching Base transfer is independently visible.
- the402's public request feed is monitored through `scripts/monitor-the402-opportunities.mjs`. Its API provider registration costs $0.01 via x402, while the free dashboard route displayed `New registrations are temporarily paused` on 2026-08-27. No registration fee was paid and no embedded wallet was created; the public feed remains watch-only until a genuinely free route reopens.
- Taskmarket's official CLI created a dedicated encrypted agent wallet and platform mailbox without using or replacing any principal wallet. Its one-time withdrawal destination is the exact disclosed Base address. Eleven escrow-backed, no-stake tasks now have original submissions: the truthful [paid-work survey](deliverables/taskmarket/TSK-KFVEVKWG.txt), [agent-API discovery report](deliverables/taskmarket/TSK-A9YNYFG0.md), [eligible-bounty API pitch](deliverables/taskmarket/TSK-XF0XEEVN.md), an exactly ten-file [x402 agent-product portfolio](deliverables/taskmarket/TSK-WAAYZDHJ/), a complete [Leaf Box brand kit](deliverables/taskmarket/TSK-20W651EW/) whose validator proves the exact requester-supplied logo pixels were preserved, a deployed [Inference Index](https://inference-index-v6.radearthonline.chatgpt.site) with reproducible MLPerf data plus free and x402-paid Lucid Agent APIs ([review handoff](deliverables/taskmarket/TSK-WAN8H9G1/SUBMISSION.md)), a reproducible [AgentWork live-MCP failure report](deliverables/taskmarket/TSK-RSTTWB3W/SUBMISSION.md) showing an orphaned request after timeout and duplicate detection, the validated [Death Gym benchmark](deliverables/taskmarket/TSK-E4RXQS7X/) submission, a clean-room [Persistent Objective plugin](deliverables/taskmarket/TSK-HQQ9T3VE/SUBMISSION.md) whose 14-test suite and isolated Codex 0.145.0 install prove bounded lifecycle, native Stop continuation, exact uninstall, one-writer locking, corruption handling, and secret/bypass guards, the interactive [Lattice Orchard](deliverables/taskmarket/TSK-4M83XEN0-infinite-garden/) infinite garden, and the interactive [Pelagic Oscillator](deliverables/taskmarket/TSK-9Z9HENXP-mathematical-creature/) mathematical creature. Their combined maximum net reward pools total 131.8125 USDC, though bounty pools may be split and selection bounties pay only their selected winners. All eleven currently have zero awards and the platform wallet balance remains zero. `scripts/monitor-taskmarket.mjs` verifies each task, submission hash, award state, platform balance, and exact withdrawal target. A two-minute settlement timer runs `scripts/withdraw-taskmarket-earnings.mjs`: it can withdraw only the full earned balance to that locked Base address after validating chain ID 8453 and official Base USDC, then requires the exact worker-to-target ERC-20 transfer in a successful receipt. A separate five-minute timer independently rechecks all four target chains. Neither submissions nor platform balances count until a matching target-chain transfer exists.
- The Taskmarket address `ten-dollar-wallet@taskmarket.dev` is a working project mailbox with inbox/read/send/reply support, not a display-only alias. It can complete ordinary external email verification without using a personal address; credentials and verification messages remain local. New marketplace audits found AgentJob temporarily offline with Vercel `DEPLOYMENT_PAUSED`, TaskForce's documented free agent-registration endpoint repeatedly returning HTTP 500, and all three Obolos work-discovery endpoints returning HTTP 500. `scripts/taskforce.mjs` performs idempotent registration and stores any eventual key only under the ignored `.taskforce/` directory. The heartbeat retries these services conservatively, while Atrest is excluded because its own docs call escrow mock mode, ClawTasks has suspended paid work, and Augmi publicly reports zero open bounties and zero paid volume.
- A free Handsel worker registration created agent `rY7JrPFLInjT-aUXdh1DY` and Base mainnet smart account `0x76E98f46Fd0D6DC90f7b232d1B392F830137A77F`; the account password and one-time worker secret remain only in ignored mode-0600 state. The public feed identifies real Circle USDC and currently lists 6.110001 USDC of nominally open work. Three $1.67 office-pipeline steps are reserved for other hired workers despite their public `Open` label, two untitled rows have no usable brief, and the remaining $1 repository smoke test cannot be claimed from a zero-balance worker. Handsel's official protocol requires at least 0.00005 ETH for mainnet gas plus a refundable 5% + $0.03 USDC bond before a claim. No user funds were supplied. `scripts/monitor-handsel.mjs` verifies the network, worker balances, task briefs, known reservations, and funding preconditions; no listing, registration, bond, or worker-wallet balance counts as target-wallet value.
- WorkProtocol agent `c0961f83-c16e-494f-9e90-f48d6ae7c0f5` was registered free with the exact Base payout address and no email, signature, deposit, or user funds. The marketplace currently has zero open jobs. Its own public stats report `volumeSettled: 0.00` and classify the prominently displayed historical 1,325 USDC as database-recorded but not verifiable on-chain; direct Base RPC checks also found no receipts for three sampled historical `payment_released` hashes. Those records are excluded. `scripts/monitor-workprotocol.mjs` checks the locked wallet and new jobs; even an `open` job is only a candidate until its specific Base escrow receipt and official-USDC transfer are independently verified before any claim.
- A dedicated Base worker wallet was created locally for Agent Bounties with its secret stored only in ignored mode-0600 state. The live canonical feed exposed one fully funded, verification-ready 1.00 USDC coding bounty with zero required external spend, but its 0.10 USDC claim bond was not optional: the worker held zero USDC and the official claim API returned `sponsorship_unavailable`. It was therefore not claimed and no user funds were requested. `scripts/monitor-agentbounties.mjs` rechecks canonical inventory, exact Base state, worker balance, and narrow wallet policy; a listing or signature is never counted, and only canonical `BountySettled` proves earnings.
- Zentience documents free agent registration with a caller-supplied Solana payout address and a public task API. `scripts/register-zentience.mjs` is prepared to register a truthful technical-work profile against the exact disclosed Solana address only after the public task endpoint passes a normal TLS and liveness check. On 2026-08-27 the custom domain served a Railway certificate for a different hostname and returned `Application not found`, so no account was created and TLS verification was not bypassed. If it returns, an open task still requires independently verifiable funded escrow before any bid or work; a database budget alone is not payment evidence.
- A separate 100 USDC gross / 92.5 USDC net Taskmarket benchmark trains an RL policy for Death Gym without any stake or paid service. The validated 20M-step baseline scored 179.5 XP on the official 49,152-world public bank. Stable long-run checkpoints improved from 238.1 XP at 249M steps, to 255.9 at 499M, 263.5 at 749M, and 272.6 at 999M; each passed the official validator and was submitted exactly once. The latest submission is `f248bcfa-d8f7-46c4-aedd-b4161da4da00`, anchored by transaction `0x20b2ee5fac5de71676b19134587789dd9fa88f904e56f700f2df0bdfc758e301` with archive SHA-256 `e314dcd3ab9f690476eeaea45fab9104431996ced3116a3a8c02c18cf703b397`. The next upload threshold is 275.6 XP. The active 5B-step run writes a checkpoint every 250M steps and will automatically resume from its final checkpoint toward 9B steps, which is the largest target projected to finish safely before expiry. A five-minute timer finds the newest stable checkpoint across both runs, evaluates it once, and submits only if it beats the recorded best by at least 3 XP (or strictly improves it within the final two hours). `scripts/monitor-deathgym.mjs` reports both services, the 9B ETA, evaluations, and exact submitted artifact hashes; official Taskmarket readback must match the archive SHA-256, and an API outage fails closed before a new submission. Compute, checkpoints, leaderboard rows, and submissions are not funds; only an awarded, withdrawn, target-chain transfer can count.
- A free journal-return bounty was started with one project-scoped feed token and a verified first fetch on 2026-08-27 UTC. `scripts/krimskrams-journal.mjs` keeps the token in an ignored local file and permits exactly one qualifying revisit on a later UTC date before truthful submission. No same-day fetch or extra token is represented as qualified.
- A registered Agent Hansa worker now has a verified project email and the listed Solana payout address bound. It monitors agent-safe tasks and zero-entry survival tournaments; cancelled credits, internal betting balances, and non-withdrawable bonuses remain excluded.
- A GitHub scanner checks recent USDC and SOL bounty issues while filtering testnets, self-funded tasks, deposits, private-key requests, external social-account work, and bounty aggregators.
- Bounty attempts must require no deposit, wallet signature, seed phrase, private key, or fabricated identity. Original submissions and payment evidence will be disclosed if work is accepted.
- Block explorers are always the authoritative record.
- Registration-mail automation uses the [Mail.tm API](https://mail.tm/) for a dedicated project inbox; credentials remain local and are never committed.

Superteam Earn 代理账号会检查官方允许代理参与的任务；其 API 凭据和领取码不会公开。任何尝试都不得要求押金、钱包签名、助记词、私钥或虚假身份；若成果获奖，将公开原创提交与付款证据。

TaskBounty 修复代理已使用项目邮箱注册，首笔奖励被官方 API 锁定为向公开的 Solana 地址支付 USDC。签名通知与五分钟轮询会捕获新悬赏；只提交可复现、带回归测试且无需垫资的原创修复，平台状态不等于到账。

If this honest little experiment made you smile, send a dollar—or less. Every tiny amount counts.

如果这个诚实的小实验让你会心一笑，可以送一美元，或更少。任何小额赠与都有帮助。
